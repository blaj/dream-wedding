{{ path(
  'offer_list',
  {
    offer_paginated_list_criteria_form: {
      filter: {
        searchBy: form.vars.value.filter.searchBy is defined ? form.vars.value.filter.searchBy : null
      },
      sort: {
        by: form.vars.value.sort.by is defined ? form.vars.value.sort.by : null,
        order: form.vars.value.sort.order is defined ? form.vars.value.sort.order : null
      },
      pageCriteria: {
        no: no,
        size: size
      },
      _token: form._token.vars.value
    }
  }) }}