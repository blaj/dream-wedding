{% extends 'dashboard-base.html.twig' %}

{% import 'macros/breadcrumbs.html.twig' as breadcrumbs %}

{% block title %}
  {{ 'cost-estimate-group-details'|trans }}
{% endblock %}

{% block page_heading %}
  <h1 class="h3 mb-2 mb-sm-0">
    {{ 'cost-estimate-group-details'|trans }}

    {{ costEstimateGroupDetailsDto.name }}
  </h1>

  <a
      href="{{ path('wedding_cost_estimate_group_update', { weddingId: weddingDetailsDto.id, id: costEstimateGroupDetailsDto.id }) }}"
      class="btn btn-primary"
      data-controller="tooltip"
      data-bs-title="{{ 'edit'|trans }}">
    <i class="bi bi-pencil"></i>
  </a>
{% endblock %}

{% block breadcrumbs %}
  {{ breadcrumbs.render([
    { text: 'wedding-planner'|trans },
    { url: path('wedding_list'), text: 'wedding-list'|trans },
    { url: path('wedding_details', { id: weddingDetailsDto.id }), text: weddingDetailsDto.name },
    { url: path('wedding_cost_estimate_group_list', { weddingId: weddingDetailsDto.id }), text: 'cost-estimate-group-list'|trans },
    { text: costEstimateGroupDetailsDto.name }
  ]) }}
{% endblock %}

{% block page_content %}
  <div class="card shadow">
    <div class="card-body">
      <table class="table">
        <tbody>
          <tr>
            <td>
              {{ 'name'|trans }}
            </td>

            <td>
              {{ costEstimateGroupDetailsDto.name }}
            </td>
          </tr>

          <tr>
            <td>
              {{ 'description'|trans }}
            </td>

            <td>
              {{ costEstimateGroupDetailsDto.description }}
            </td>
          </tr>

          <tr>
            <td>
              {{ 'cost-estimates'|trans }}
            </td>

            <td>
              {% for costEstimateName in costEstimateGroupDetailsDto.costEstimateNames %}
                <span class="badge text-bg-primary">
              {{ costEstimateName }}
            </span>
              {% endfor %}
            </td>
          </tr>
        </tbody>
      </table>

      <a href="{{ path('wedding_cost_estimate_group_list', { weddingId: weddingDetailsDto.id }) }}">
        {{ 'back-to-list'|trans }}
      </a>
    </div>
  </div>
{% endblock %}